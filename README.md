# Breash Supplies

Marketing website for Breash Supplies Ltd., built with Next.js and ready to deploy on Vercel.

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:3000.

## Deploy to Vercel

Import this repository into Vercel. It will automatically detect Next.js and use `npm run build`.
